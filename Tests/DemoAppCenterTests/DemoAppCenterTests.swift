    import XCTest
    @testable import DemoAppCenter

    final class DemoAppCenterTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DemoAppCenter().text, "Hello, World!")
        }
    }
